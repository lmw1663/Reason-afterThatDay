# 의사결정 로그 — 2026-05-08

> 한 세션 동안 사용자(leeminwoo)와 합의한 결정 묶음.
> 형식: **질문 → 답 → 결과(커밋)**.

## 1. UX 일관성 — 뒤로가기·종료 동선

### 1.1 결정 나침반 진입/결과 화면
- **상황**: 첫 화면(`compass/index.tsx`)에 뒤로가기 부재, 결과 화면(`action.tsx`)에 "일기 쓰러 가기"·"홈으로" 두 동선 공존
- **답**: 뒤로가기 추가 / 결과 화면은 "홈으로" 단일 버튼
- **결과**: `aeffa02` — 진입 화면 BackHeader, 결과 화면 단일 CTA

### 1.2 일기·미니·관계 분석 첫 화면 뒤로가기
- **질문**: 뒤로가기 빠진 곳 어디까지 추가?
- **답**: "흐름 일관성 깨진 2개만"
- **결과**:
  - `be11d45` — 일기 1/4
  - `c99471e` — 분석 1/5(reasons), 일기 미니 작성
  - `memory/reflect.tsx`는 종료 동선 명확해 제외

### 1.3 갑자기 떠올랐어 모달 — 마무리 동선
- **상황**: step 4에 "일기 쓰러 가기" + 텍스트 링크 "뒤로 가기"
- **답**: 일기쓰기 동선 제거, 홈 복귀 단일
- **결과**: `e5d4bcf` — `IntrusiveMemoryModal` step 4 단일 버튼화

---

## 2. UX 점검 라운드

### 2.1 점검 범위
- **답**: 네비게이션 일관성 + 로딩/에러/빈 상태 + 클릭 피드백/접근성 (3축)
- **결과**: 🔴 3건(쿨링 체크인·회고 fail-open + needle retry 침묵) + 🟡 자잘 발견

### 2.2 어디까지 고칠지
- **답**: "🟡도 포함해서 한 번에"
- **결과**: `fef9637` — ErrorToast catch 보강 + 토스트 노출

### 2.3 멱등성 위험 발견 후 처리
- **상황**: opus 검증에서 자동 retry가 비멱등 API 호출 → 데이터 중복 위험 발견
- **답**: "검증내용 고쳐야될점 너가 알아서 수정하고 진행해줘" (위임)
- **결과**: 일단 retry 버튼 빼고 안전 커밋. DB 멱등성 별도 라운드로.

---

## 3. DB 멱등성

### 3.1 마이그레이션 040 작성 결정
- **상황**: cooling_reflections·checkin_responses 비멱등 구조
- **답변(묵시적)**: 정통 마이그레이션 추가 진행
- **결과**: `cb85545` — UNIQUE 제약 + atomic dedupe RPC 함수

### 3.2 운영 적용 push 범위
- **상황**: 040 외에 037~039(질문 풀 v2 인프라)도 미적용 발견
- **답**: 처음 "🟡 040만" → CLI 한계 설명 후 → "🟢 4개 모두 push"
- **결과**: 운영에 037~040 일괄 적용. Local=Remote 동기화 확인.

---

## 4. 회복의 결 (RSE 화면)

### 4.1 카드 분기 정책
- **답**: "측정 1회만이면 처음 결만 / 같으면 지금 결만 / 다를 때만 둘 다"
- **결과**: `72b72c0` — `ComparisonCard` 4단계 분기

### 4.2 화면 안내 형식
- **상황**: 들어온 사용자가 무슨 화면인지 즉시 파악 어려움
- **답**: "🟢 헤더 아래 설명 1줄 추가"
- **결과**: `36ace8a` — "짧은 심리검사 3개(우울·불안·자존감)로 시간에 따른 변화를 따라가 봐"

### 4.3 RSE 기능 분석 보고
- **답**: "📝 보고서 md로 정리"
- **결과**: `52c38f6` — `docs/recovery-trace-rse.md` 생성

---

## 5. 인프라

### 5.1 온보딩 재진입 문제
- **상황**: `setOnboardingCompleted(true)`가 Zustand만 갱신, DB 미반영 → 매번 재로그인 시 온보딩 재진입
- **답변(묵시적)**: 즉시 fix
- **결과**: `e8adbae` — DB 영속화 + 로그인 가드. opus 검증 6/6 🟢

### 5.2 선행 리스크 추적 범위
- **상황**: opus 검증이 "breakup_date NOT NULL"을 false-positive로 보고
- **답**: "✅ 이번 건 끝내고 다른 일 진행"
- **결과**: 031에서 이미 nullable 처리 확인. 추가 작업 없음.

### 5.3 모바일 푸시 알림 페어링
- **상황**: PushNotification이 "Mobile push not sent (Remote Control inactive)"
- **단계**:
  1. /config의 "Push when Claude decides"는 켜져 있음
  2. /remote-control 실행 → "Enabled Remote Control for all sessions"
  3. 모바일 앱 Code 탭 진입 후 페어링 완료
- **결과**: AskUserQuestion·PushNotification 모바일 미러링 정상 작동 확인

---

## 6. 제안 / 미래 작업

### 6.1 홈 위젯 도입
- **상황**: 사용자가 "갑자기 떠올랐어를 OS 위젯으로 빼고 홈에서는 제거" 제안
- **답변**:
  1. 홈 칩 제거는 신중 (위젯 미추가 사용자 차단 위험)
  2. 위젯 방향: "📝 아이디어 도큐멘트로 정리만" — 즉시 구현 X
- **결과**: `b43bb29` — `docs/widget-proposal.md` (단일 액션·메뉴형·하이브리드 안 비교)

### 6.2 사용자 답변 모음 화면 (현재 진행 중)
- **답**: 의사결정 로그 + 앱 화면 둘 다 필요
- **상태**: 본 문서가 첫 번째 산출물. 앱 화면은 별도 작업

---

## 7. 미커밋 발견 사항 (참고)

세션 종료 시점 워킹 트리 untracked 파일:
- `hooks/useJournalQueue.ts`
- `hooks/__tests__/useJournalQueue.test.ts`
- `utils/journalQueueSkip.ts`

직전 [Q-1]·[Q-2] 작업의 일부로 추정. 커밋 누락인지 의도된 미커밋인지 사용자 확인 필요.

---

## 부록: 커밋 타임라인

| 해시 | 변경 |
|---|---|
| `aeffa02` | 결정 나침반 진입 뒤로가기 + 결과 화면 단일 버튼 |
| `be11d45` | 일기 1/4 뒤로가기 |
| `c99471e` | 분석 1/5 + 일기 미니 뒤로가기 |
| `fef9637` | ErrorToast catch 보강 + 쿨링 토스트 노출 |
| `cb85545` | 040 멱등성 마이그레이션 + retry 복원 |
| `72b72c0` | 회복의 결 카드 분기 |
| `36ace8a` | 회복의 결 헤더 캡션 명확화 |
| `52c38f6` | RSE 기능 분석 문서 |
| `e5d4bcf` | 갑자기 떠올랐어 모달 단일 버튼 |
| `e8adbae` | 온보딩 재진입 차단 |
| `b43bb29` | 홈 위젯 제안 도큐멘트 |
